# Proton Plugin Manager

## Notes

### Loading

> `plugdir` = `Constants.PLUGINDIR`

Plugins will be loaded from `plugdir`. The contens of each folder inside `plugdir` will be the `lib<plugin_name>.so` and the `<plugin_name>.plugin` files.
